import 'package:flutter/material.dart';

class UpdateEggPriceScreen extends StatelessWidget {
  const UpdateEggPriceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Update Egg Price')),
      body: const Center(child: Text('Update Egg Price Form Here')),
    );
  }
}
